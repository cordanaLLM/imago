# Explicit pytest runner selection (praetor adoption requires pytest.ini or a
# Python 3.14 pin to identify the runnable test gate). Keep in sync with tests/.
[pytest]
minversion = 7.0
testpaths = tests
pythonpath = .
python_files = test_*.py
addopts = -v --strict-markers
