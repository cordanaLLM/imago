# Proposed praetor archetype for OS image forges (Packer, mkosi, kernel and
# provisioner pipelines). Drafted in cordanaLLM/imago for upstream adoption in
# cordanaLLM/praetor (.config/archetypes/os-image.yaml). Until praetor ships
# it, the repository declares the interim profile "gitops-infra".
#
# Schema: praetor profile definition (docs/guides/archetype-authoring.md).
# Complexity bounds follow the Holzmann Power of 10 adaptation this repository
# already enforces on shell provisioners and Python tests (60 lines per function).
id: "os-image"
name: "OS Image Forge"
description: "Hardened, reproducible OS image pipelines (Packer/QEMU, mkosi, UKI) with declarative version manifests, shell provisioners under Power-of-10 bounds, and signed image artifacts"
runtime: "image"

complexity:
  max_cyclomatic: 10
  max_cognitive: 12
  max_func_loc: 60
  max_statements: 40

branch_protection:
  enforce_linear_history: true
  require_signed_commits: true
  required_approving_reviewers: 1
  dismiss_stale_reviews: true

supply_chain:
  slsa_level: 3
  enforce_cosign: true
  require_sbom: true

linters:
  - "packer"
  - "shellcheck"
  - "shfmt"
  - "yamllint"
  - "trivy"
  - "gitleaks"
  - "codespell"

devcontainer_features:
  - "ghcr.io/devcontainers/features/common-utils:2"
  - "ghcr.io/devcontainers/features/go:1"
  - "ghcr.io/devcontainers/features/python:1"
